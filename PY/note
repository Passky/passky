什么是他妈的 .format (?)
动态语言  不用声明
[listname].insert/append
-1是倒数第一个 可以往前类推
可以list里套list 构成多维 list->[] tuple->()  <- 声明
tuple= const list
(只有一个元素的tuple,需要加','消除歧义,而无元素仅需要[([内无内容])])
tuple内可以嵌套一个元素可变的list 类似于指针  取单用序号
dict->{key:值,} 一一对应 类似map    取单时->dictname['key'] ->此时可直接用等号关联  
|| 判断存在 key in dictname or dictname.get(key)
删除 dictname.pop(key)
⬆吃内存;空间换时间
